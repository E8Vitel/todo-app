import React from 'react';

function TodoList() {
    return (
        <div>
            <h2>Lista de tareas</h2>
            {/* Aquí va la lista de tareas */}
        </div>
    );
}

export default TodoList;